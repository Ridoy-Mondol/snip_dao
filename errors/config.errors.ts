import { Name } from "proton-tsc";

export function ERROR_CONFIG_ENTRY_NOT_FOUND(): string {
  return `Config entry not found `;
 }

export function ERROR_NO_MATCHING_PERIOD_LOCK_FOUND(): string {
  return `No corresponding locking period found`;
 }

export function ERROR_REDUCING_PERIOD_NOT_PERMITED(): string {
  return `You can't lower lock period`;
 }


export function ERROR_MISSING_REWARD_ACCOUNT(): string {
  return `Missing reward account`;
 }

